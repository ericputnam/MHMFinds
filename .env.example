# ============================================
# DEVELOPMENT ENVIRONMENT VARIABLES
# ============================================
# Copy this file to .env.local and fill in your values
# ============================================

# Database
DATABASE_URL="your-database-url"
POSTGRES_URL="your-postgres-url"
PRISMA_DATABASE_URL="your-prisma-accelerate-url"
DIRECT_DATABASE_URL="your-direct-database-url"

# NextAuth.js
NEXTAUTH_URL="http://localhost:3000"
NEXTAUTH_SECRET="your-nextauth-secret-run-openssl-rand-base64-32"

# OAuth Providers (Optional - only if using social login)
GOOGLE_CLIENT_ID="your-google-client-id"
GOOGLE_CLIENT_SECRET="your-google-client-secret"

DISCORD_CLIENT_ID="your-discord-client-id"
DISCORD_CLIENT_SECRET="your-discord-client-secret"

# OpenAI API (Optional - for AI features)
OPENAI_API_KEY="your-openai-api-key"

# Perplexity API (Optional - for research and creator discovery)
PERPLEXITY_API_KEY="your-perplexity-api-key"

# Vector Database (Optional - Pinecone)
PINECONE_API_KEY="your-pinecone-api-key"
PINECONE_ENVIRONMENT="your-pinecone-environment"
PINECONE_INDEX_NAME="modvault-embeddings"

# Content Source APIs (Optional)
PATREON_CLIENT_ID="your-patreon-client-id"
PATREON_CLIENT_SECRET="your-patreon-client-secret"

# Redis (Optional - for caching and sessions)
REDIS_URL="redis://localhost:6379"

# Elasticsearch (Optional - for full-text search)
ELASTICSEARCH_URL="http://localhost:9200"

# File Storage (Optional - AWS S3)
AWS_ACCESS_KEY_ID="your-aws-access-key"
AWS_SECRET_ACCESS_KEY="your-aws-secret-key"
AWS_REGION="us-east-1"
AWS_S3_BUCKET="modvault-assets"

# Email Service (Optional - SendGrid)
SENDGRID_API_KEY="your-sendgrid-api-key"
FROM_EMAIL="noreply@modvault.com"
EMAIL_FROM="noreply@musthavemods.com"

# ============================================
# MONETIZATION AGENT NOTIFICATIONS
# ============================================
# Slack webhook for agent alerts (PRD-19)
SLACK_WEBHOOK_URL="https://hooks.slack.com/services/xxx/yyy/zzz"

# Notification settings
NOTIFICATION_DIGEST_HOUR=9
NOTIFICATION_TIMEZONE="America/New_York"

# Analytics (Optional)
GOOGLE_ANALYTICS_ID="your-ga-id"
MIXPANEL_TOKEN="your-mixpanel-token"

# Monitoring (Optional)
SENTRY_DSN="your-sentry-dsn"

# Development Settings
NODE_ENV="development"
DEBUG="modvault:*"

# Privacy Level (default, stealth, conservative)
PRIVACY_LEVEL="default"

# Scraper Configuration
USE_AI_SCRAPER=false

# Ollama (Local AI) Configuration
USE_OLLAMA=true
OLLAMA_BASE_URL=http://localhost:11434/v1
OLLAMA_MODEL=llama3.2:3b

# ============================================
# STRIPE (Required for subscriptions)
# ============================================
# Get from: https://dashboard.stripe.com/test/apikeys (use TEST keys for dev)

STRIPE_SECRET_KEY=sk_test_your_stripe_secret_key
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_test_your_stripe_publishable_key
STRIPE_WEBHOOK_SECRET=whsec_your_webhook_secret_from_stripe_listen

# Stripe Price IDs (create products in Stripe Dashboard -> Products)
NEXT_PUBLIC_STRIPE_CURIOUS_MONTHLY_PRICE_ID=price_your_monthly_price_id
NEXT_PUBLIC_STRIPE_CURIOUS_QUARTERLY_PRICE_ID=price_your_quarterly_price_id
NEXT_PUBLIC_STRIPE_CURIOUS_SEMIANNUAL_PRICE_ID=price_your_semiannual_price_id
NEXT_PUBLIC_STRIPE_CURIOUS_ANNUAL_PRICE_ID=price_your_annual_price_id

# ============================================
# ADMIN CREDENTIALS (Optional - for admin login)
# ============================================
ADMIN_USERNAME="adminuser45"
ADMIN_PASSWORD="your-strong-admin-password"

# ============================================
# SETUP INSTRUCTIONS
# ============================================
# 1. Copy this file to .env.local: cp .env.example .env.local
# 2. Fill in your database URLs (DATABASE_URL, DIRECT_DATABASE_URL)
# 3. Generate NEXTAUTH_SECRET: openssl rand -base64 32
# 4. Add your Stripe TEST keys from dashboard.stripe.com
# 5. Run Stripe webhook listener: stripe listen --forward-to localhost:3000/api/webhooks/stripe
# 6. Copy webhook secret from Stripe CLI to STRIPE_WEBHOOK_SECRET
# 7. Run database migrations: npm run db:push
# 8. Start dev server: npm run dev
