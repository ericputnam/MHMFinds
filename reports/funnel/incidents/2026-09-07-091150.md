# Incident 2026-09-07 09:11 — verification failed; rolled back

**Mode:** after-merge · **Label:** Quinn: PR #55 record approvals 5 A / 4 option A, close Q2 · **Commit:** 5d1b8da448c4bc6cac2ff624c4b1dd89c8b8d60e · **Deployment:** https://mhm-finds-dw5l-l5tet3v1j-ericputnams-projects.vercel.app

## Failures
- smoke-render: / -> 1 uncaught page error(s): Converting circular structure to JSON @ at track (https://scripts.mediavine.com/tags/prebid/7c6f98787b60bed4cf30.min.js:

## Action taken
Rolled production back to https://mhm-finds-dw5l-k0sjsbndt-ericputnams-projects.vercel.app (the deployment that had passed at 09:07). Re-check PASSES.

## Resolution (Quinn, 2026-09-07 09:30) — false alarm, closed

- **Root cause:** the failing error is Mediavine's own prebid bundle throwing `Converting circular structure to JSON` in `track()` on the homepage. The rolled-back deployment `5d1b8da` differs from the deployment that passed at 09:07 (`f7820cd`) by two markdown files (`operator-queue.md`, the tier-relaunch draft) — no code, no config. The same error was recorded as *transient* on the 09:07 PASS; at 09:14 it happened to reproduce on both loads, which exhausted the one retry.
- **Impact:** none. The rollback re-promoted `f7820cd`'s build, which is byte-identical in code. Production kept serving the Patreon-membership build throughout; 5xx = 0; ad anchors and blog markers passed on every check.
- **Fix (PR #56, `5b7ee43`, verified live 09:21):** `smoke-render.ts` now classifies an uncaught error whose top frame is on another origin (e.g. `scripts.mediavine.com`) as a third-party warning (`thirdPartyErrors`, shown as `(3rd-party N ⚠)`) instead of a hard failure. Structural checks (HTTP 200, Mediavine loader, `aside#secondary`, `.mv-ads`, page text, Application-error boundary) still decide pass/fail; an error with no frame or on our own host is still a hard failure. Verified: the incident's error string classifies as third-party, own-host and no-frame samples stay hard; full production render 7/7 pass.
- **Tier 1 merges:** unblocked from 09:21 on 2026-09-07.
- **Note 2026-09-08 (Quinn):** the runner copied the operator tree's pre-resolution copy of this file over the committed one; this version restores the incident header plus the resolution so the file is complete.
