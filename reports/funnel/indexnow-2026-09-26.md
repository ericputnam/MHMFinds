# IndexNow push — 2026-09-26 (E115, Sage)

Explicit live submit of the 09-24/09-25 page classes to Bing / Yandex / Seznam / Naver
via `scripts/agents/indexnow-submit.ts` (E47/E52/E95 tooling; no code change).

## Runs today (from `logs/indexnow.log`, one line per run, fixed key order)

| When (UTC) | Caller | Mode | Status | URLs | collections/hubs | mods (days) | creators | HTTP |
|---|---|---|---|--:|--:|--:|--:|--:|
| 2026-09-26T10:43:03Z | daily runner step 0c2 (`--apply --days 2`) | live | OK | 55 | 28 | 27 (2) | 0 | 200 |
| 2026-09-26T13:19:41Z | Sage, this move (`--apply --creators --days 3`, alarm-bounded 150 s) | live | OK | 596 | 28 | 27 (3) | 541 | 200 |

`dropped=0` on both runs (every URL passed `isCanonicalUrl`: https, apex host, trailing slash).
`cap=1000` on the creators run (CREATORS_HARD_CAP), not hit.

## What the 596 URLs are

- **28 collection/hub URLs** — `/`, `/games/sims-4/`, and all 26 registry collections, so
  `/games/sims-4/kitchen-cc/` (E109, live 09-25) was pushed twice today. `/games/sims-4/bathroom-cc/`
  is not on `origin/main` at branch time (0 matches in `lib/collections.ts`) and was not pushed — never
  submit a URL that would 404.
- **27 `/mods/[id]/` pages** created since 09-23 (isNSFW=false, isVerified=true — the sitemap-mods
  population).
- **541 `/creator/[slug]/` pages** — the whole `listCreators()` population (E95/E97), i.e. every page
  the 09-25 `llms-full.txt` change (#176) now links.

## Not pushed, and why

- The `/mods/[id]/` pages touched by #175 ("More from <creator>" server-rendered block) are **every**
  mod page (~16.5K). The script's hard cap is 500 (1,000 with `--creators`) by design — an enumeration
  bug must be caught by the cap, not by Bing's rate limiter. Bing re-crawls those on its own schedule
  from `sitemap-mods.xml` (`lastmod` = `updatedAt`). A `--since-updated` mode would be a separate move.
- Blog guides are not in the IndexNow population at all (collections, mods, creators only). The 10 guides
  modified 09-04→09-17 earned 66 Bing sessions in 09-18→09-24 (GA4, read this morning before the
  network degraded); a `--guides` leg via `lib/seo/wpGuides.ts` is queued in the ideas inbox as the next
  Sage move, not widened into this one.

## Before numbers (scoreboard 2026-09-26, GA4 09-18→09-24)

- ai_referral 7d **274** (prev 329) — the entire drop is chatgpt.com 304 → 248; gemini 19 → 20,
  copilot 4 → 4, claude.ai 0 → 1, perplexity 1 → 1.
- google_organic 7d **2,753** (prev 2,759); bing_organic 7d **16,284** (prev 16,042).
- Bing-organic sessions landing on `/creator/*` 7d: 0 (09-17→09-23, E95 baseline).

## Read on / keep rule

Read **2026-10-03** (7d 09-26→10-02) alongside E95: KEEP if Bing-organic landings on `/creator/*`
≥ 20 **and** bing_organic 7d ≥ 95% of 16,284; otherwise the creators push stays occasional
(not wired into the daily runner) and E95's verdict stands.

— Sage, Search & AI
