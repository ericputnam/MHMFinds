# Pip — Distribution — Playbook

Your memory across runs. Append one dated entry per run, newest at the top,
**with a number**. "I think it worked" is not a learning. Covers channels: what pin/post types drive sessions, pinner incidents, format tests, new-channel results.

Entry format:

```
## YYYY-MM-DD
- Tried: …  (tier, PR/link)
- Before → after: <metric> <n> → <n> (<window>)
- Verdict: KEEP / KILL / MORE DATA (read on <date>)
- Next time: one sentence
```

## Kill log
_(ideas you tried that did not work — never re-propose without saying what changed)_

---

## 2026-09-16
- Tried: E56 — third stranded-pin revival (T0: execution of the T1-approved script, no code change). `revive-stranded-pins.py --per-day 14 --days 14 --experiment E56 --apply` after a clean dry run: **196 rows re-dated 2026-09-16 → 09-29 at 14/day**, 59 destinations at 1 pin/destination/day, original dates 2025-01-01..2026-05-12; dropped before writing: 37 `blog.*` host rows, 3 missing-field, 1 dead image, 2 dead-section, 0 duplicate images, 0 dead destinations. Ledger `reports/funnel/pin-revival-2026-09-16.json`. Sizing rule written before the dry run: stacked on E26+E46 this is 38/day through 09-23, 28/day 09-24→27, 14/day 09-28→29 — under the ~72/day cron ceiling and at the 47–48/day already observed 09-13/14. Also graded E1 and traced the writer drought (below).
- Before → after: `check-pinner.sh` step 2 runway **≈7.7 d (230 rows ÷ 29.7/day) → > 14 d (426 rows ÷ 29.7/day)**; schedulable 6 → 20; stranded 1,443 → 1,247; unposted total 1,673 unchanged (re-dated, nothing created). `repair-pin-sections.py --check --days-ahead 14`: all 426 rows incl. 14 days ahead have a live section. Pinterest 7d 56,575 (−3.8%, 09-08→09-14); pins created 47/24h, 208/7d. Pinterest sessions 7d to the 59 E56 destinations: baseline to be pulled from GA4 09-09→09-15 at the 09-30 read (not pre-read today, budget).
- Verdict: **E1 KILL** — the 7 catalog pages pinned 09-04 (hair-cc, tattoos, holidays-cc, clutter, y2k-cc, vampire-cc, poses) read 3/0/0/0/0/0/0 Pinterest sessions for 09-09→09-15 vs the ≥20 rule (0 of 7 pass; later catalog pins makeup-cc 1, witch-cc 0, shoes-cc 1, decor-cc 1). Direct catalog-URL pins do not move sessions at 1–2 pins/page. The real signal in the same read: Pinterest sessions to *all* `/games/sims-4/*` fell **1,897 → 723/7d** (pregnancy-mods 816→383, female-clothes 697→159) because those pages are fed by blog-post pin internal links and the 09-04 blog batch drained. E56 MORE DATA (read 2026-09-21 cadence: keep if `pinsCreated24h` ≥ 30 every day 09-17→09-21 and step 6 FAILs 0 times; read 2026-09-30 sessions: keep if Pinterest 7d to the 59 destinations ≥ +10% over 09-09→09-15; rollback `--rollback reports/funnel/pin-revival-2026-09-16.json --apply`).
- Next time: **the writer drought is an absence, not a failure.** BigScoots crontab (read-only): 06:00 `run_pinterest_daily_server.py` (token + `daily_pin_poster_server.py --supabase-only` = banner rows), */20 poster, 23:00 banner poster — **nothing runs `posts_2_supabase_server.py`**, and no log on the server has ever contained `Supabase sync: inserted`. The WP plugin `mhm-pin-scheduler` has no publish hook and no WP-cron, only `wp_ajax_mhm_*` admin buttons; the 268 rows of 09-04 08:36Z were a human clicking "schedule" and nobody has since (5 posts published, 0 rows). Fix is one of two Tier 2 items for the operator: (1) ask the writer to run the plugin's schedule step per post (zero code), or (2) a cron line on BigScoots `15 6 * * * cd ~/domains/blog.musthavemods.com && python3 posts_2_supabase_server.py --count 10 >> cron_posts_2_supabase.log` after one `--dry-run` — pair it with the Q8 patch because of its known `blog.*` host bug (~lines 261/393). Revivals are a bridge: after E56 the reachable non-blog stranded pool is ~1,247 − 203 blog ≈ 1,000 rows, about five more 196-row slices, then the queue is empty for good without a writer.

## 2026-09-15
- Tried: E51 — pinner queue flag reads **inventory runway** (unposted rows dated `[today-14d, today+14d]` ÷ pins/day from Pinterest `created_at`, 7d mean) instead of "schedulable today" (T0, PR opens today). `assessRunway()`/`runwayExitCode()` in `pinner-liveness-lib.ts`; `funnel-scoreboard.ts` and `check-pinner.sh` step 2 both consume it, 🟡/WARN only when runway < 3 d or inventory is 0, liveness stays the only 🔴/FAIL path. `check-pinner.sh` step 1 now pages `/v5/pins` up to 3× like the scoreboard (one page of 100 read 96/7d → 13.7/day → "> 14 days"; three pages read 162/7d → 23.1/day → 11.0 days). 12 new tests (25 total) incl. constant-agreement guards across the TS/shell boundary. Also graded E20.
- Before → after: scoreboard Flags `🟡 Pinner: only 6 pins schedulable` → `🟢 none` (scratch run 11:00Z: "inventory runway ≈ 12 days (278 rows dated through +14d ÷ 23.1/day observed; 30 still schedulable today)"). Direct reads 2026-09-15 ~10:45Z: unposted by Post Date 09-15 **6**, 09-16..09-23 **24/day**, 09-24..09-27 **14/day** (inventory 254); posted rows dated 09-15 already **18**; Pinterest created/day 09-12 29, 09-13 48, 09-14 35, 09-15 19 (partial), 39/24h, 162/7d. Writer plugin: newest row id 11430 (09-08 catalog pin), last writer-plugin rows 11419/11420 at 09-04 08:46 — **0 rows from 6 blog posts in 7d** incl. today's sims-4-candle-cc. Between the two reads stranded fell 1468 → 1443 and schedulable 6 → 30: 25 rows were re-dated into the window by something other than this move.
- Verdict: E20 KEEP the schedulable/stranded query split (caught the true 0 on 09-08/09), KILL the `< 20 schedulable` threshold (6 vs 39 posted/24h = −85% on the ±20% rule; false 🟡 on 09-10 [10], 09-14 [7], 09-15 [6]; 0 false FAILs). E51 MORE DATA (read 2026-09-25: expect a *true* 🟡 low runway around then as E46 thins to 14/day 09-24 and ends 09-27; keep if the flag fires only then and on no metered morning before it).
- Next time: a drip-dated queue makes any point-in-time count a residue, not a buffer — the poster drains today's 24 in ~8 h then idles until midnight, so a morning read of "schedulable" is whatever is left of today's allotment. Flag on inventory ÷ rate. Also: the shell mirror sampled 1 page while the scoreboard sampled 3 — same signal, different floor, and the bias ran toward *not* flagging; when two consumers share a rate, guard the sample size in the test, not just the threshold. The real open problem is upstream: the writer plugin has produced no queue rows since 09-04, so after 09-27 the queue is E46 residue only — trace `mhm-pin-scheduler` on the blog (T2 if it needs the WP admin) before the 09-25 read, or the next true 🟡 is a real outage.

## 2026-09-10
- Tried: E26 executed — `python3 scripts/agents/revive-stranded-pins.py --apply` after `--self-test` (6/6) and a dry run. 140 stranded rows (unposted, Post Date 2026-02-22..2026-05-12) re-dated forward at 10/day across 2026-09-10 → 2026-09-23. Ledger `reports/funnel/pin-revival-2026-09-10.json` (T0 execution of the already-merged T1 script, PR #69).
- Before → after: pinner **schedulable 0 → 10** today (the other 130 become schedulable on their own dates), **stranded 1,853 → 1,713**, unposted total unchanged at 1,853 — nothing was created or deleted, only re-dated. Filters: 600 fetched, 0 missing fields, 0 duplicate images (internal or against the posted set), 56 destination URLs live / 0 dead, 1 dead image dropped. Allocator held 1 pin per destination per day across 14 distinct destinations and 10 boards; cadence effect +10.0 pins/day on top of new-post pins (~146/7d), against ~72/day cron capacity. Pinterest sessions 7d baseline 58,784 (−1.2% WoW).
- Verdict: MORE DATA (read 2026-09-25 — Pinterest sessions 7d, keep if ≥3 pts over the seasonal taper; rollback `python3 scripts/agents/revive-stranded-pins.py --rollback reports/funnel/pin-revival-2026-09-10.json --apply`)
- Next time: the revived slice is 28/140 pins (20%) pointing at `blog.musthavemods.com/...` instead of the apex — those land on the proxied duplicate rather than the canonical page, so their sessions attribute to a URL the rest of the funnel does not optimize. The generator bug is in MHMUtils `posts_2_supabase_server.py` (~lines 261/393), outside this repo; fix the writer before reviving another slice, or every future batch re-strands 1-in-5 pins on the wrong host. Also note check-pinner still reads [WARN] at 10 schedulable — the threshold was written for a full queue, not for a deliberately-metered 10/day drip.

## 2026-09-09
- Tried: E26 — `scripts/agents/revive-stranded-pins.py` (T1, PR opens today, 24h veto): re-dates the newest 140 stranded queue rows (unposted, Post Date before the poster's 14d floor) forward at 10/day for 14 days. Dry-run default, `--apply` required, HARD_CAP 200, `Is Posted=false` in the PATCH filter (not just the select), duplicate-image guard against the posted set, HEAD liveness check on every destination and image URL, and a JSON ledger so rollback is one command. Also `--self-test` (6 offline allocator assertions, no network/credentials).
- Before → after: pinner queue 0 schedulable / 1,879 stranded (2026-09-09 scoreboard), cadence ~19 pins/day all from new blog posts. Dry run selects 140 rows, original dates 2026-02-22 → 2026-05-12, across 14 distinct destinations, 1 dead image URL dropped, 0 duplicate images against the posted set, 0 dead destinations. Planned cadence ~29/day for 14 days against a cron capacity of ~72/day (1 pin / 20 min).
- Verdict: MORE DATA (read 2026-09-25 — Pinterest sessions 7d, baseline 58,782 for 2026-09-01→09-07)
- Next time: the stranded slice held only **15 distinct destination URLs across 200 rows**, so the obvious "take the newest 140" would have fired 10 pins at one article in a single morning — the shape Pinterest scores as spam. Volume moves in a pin queue are never just a count; check the *concentration* of destination and board before scheduling, and put the cap in the allocator rather than trusting the input to be diverse. The round-robin version lands exactly 1 pin per destination per day.

## 2026-09-08
- Tried: pinner backlog metric corrected to "schedulable" — the poster (MHMUtils/supabase_pin_poster_server.py) only reads rows with Post Date inside [today-14, today]; both the scoreboard and check-pinner.sh were counting every unposted row. Also queued the decor-cc catalog pin (id 11430). (T0, PR #60)
- Before → after: reported backlog 1,879 with schedulable 0 → "1 schedulable, 1,879 stranded"; check-pinner step 2 [OK] → [WARN]; scoreboard Flags 🟢 none → 🟡. Posted 136 pins/7d, all from new blog posts. All 1,879 stranded rows have images that have never been posted (0 duplicates against the posted set).
- Verdict: MORE DATA (read 2026-09-15 monitoring, 2026-09-22 decor-cc pin)
- Next time: a health check whose threshold sits far below any value the metric can reach is decoration, not monitoring. Both my earlier pinner checks (E14, E15) passed a queue that could post nothing — I validated the query, never the threshold against the real distribution. Ask "what value would make this fire, and can the metric get there?" before shipping any check. Same trap produced the hardcoded catalog ID range, fixed in the same PR.

## 2026-09-07
- Tried: (a) token-manager port — scripts/agents/pinterest-token-status.py obtains a valid Pinterest token (imports MHMUtils pinterest_token_manager, falls back to an embedded stdlib port when `requests` is missing) and check-pinner.sh now asks it instead of trusting the stored string; (b) catalog pins for the two collection pages shipped since E1 — /games/sims-4/makeup-cc/ (id 11428) and /games/sims-4/witch-cc/ (id 11429) inserted into n8n_pinterest_posts. (T0, PR #50)
- Before -> after: check-pinner.sh token step FAIL (HTTP 401, 2026-09-05) -> OK/VALID (2026-09-07, backend=embedded-port; the runner's python3 has no `requests`, so the import-only path would have been dead on every run). Refresh-token TTL 363 days. Catalog batch 7/9 posted, 2 pending. Pinterest sessions to makeup-cc and witch-cc: 0 and 0 (all sources, GA4 7d 2026-08-30 -> 2026-09-05).
- Verdict: MORE DATA (read 2026-09-14 token, 2026-09-21 pins)
- Next time: the 09-05 401 was recoverable staleness, not an outage — a monitor that cannot renew a credential will cry wolf about a pipeline that is fine. And 2 days after E1's pins posted, all 7 pages still showed ~0 Pinterest sessions (hair-cc 13/7d, poses 37/7d, none from Pinterest): pin -> session lag is weeks, so never read a catalog-pin experiment before day 14.

## 2026-09-02
- Tried: Pinterest read-back via GA4 landingPage x source analysis (7d 2026-08-25 to 2026-08-31); 437 distinct Pinterest landing pages analyzed. Two decisions written to experiments.md (DQ-1: not-set label correction; DQ-2: catalog pinning gap baselined). (T0, PR for experiments.md + pip.md)
- Before -> after: Pinterest sessions/7d to catalog pages: pregnancy-mods 812, female-clothes 695, male-clothes 137, body-presets 127, goth-cc 42, skin-details 42, cottagecore-cc 19; hair-cc/tattoos/holidays-cc/clutter/y2k-cc/vampire-cc/poses = 0
- Verdict: MORE DATA -- read on 2026-09-16; catalog pins not yet queued (that is the next T0 move)
- Next time: do not label (not set) landing-page block as bot noise. ~61% is Pinterest app traffic (referrer stripped before GA4 tag fires). True unverified block is Bing organic (not set) 802/7d. The four catalog pages getting Pinterest traffic arrived there via blog-post pin internal links, not direct catalog-URL pins.

## 2026-09-01
- Tried: nothing yet — team chartered today. Read `../charter.md`, `../autonomy.md`, `../operating-model.md`, `../targets.json`, and `reports/growth/fact-base-2026-09-01.md` before your first move.
- Before → after: baseline in `../targets.json`
- Verdict: —
- Next time: your first move should be the top item in your agent file's "levers" list unless the scoreboard shows a 🔴 in your area.

## 2026-09-12

**Tried:** Diagnosed the scoreboard's "Pinner last posted 2026-09-10" 🔴 (E36). Facts from Supabase `n8n_pinterest_posts`, the Pinterest v5 API, and a read-only SSH tail of `supabase_pin_poster.log` on BigScoots. Cron, token and uptime were all fine; the poster had run every 20 minutes since 2026-09-10 07:00 CDT and failed 138 times in a row on the same row (id 8007, board 762656586838201454 "Black Sims 4 CC Hair", section 5483319435714435392 "Black Sims 4 CC") with Pinterest `404 code 2031 couldn't find this board section`. The poster is batch-size 1, oldest-first, and never marks or skips a failed row, so one dead board section blocked all 56 schedulable pins. The dead row was one of my own E26 revivals: E26 checked destination and image URLs but not board sections. Shipped `scripts/agents/repair-pin-sections.py` (dry-run default, `--check` monitor mode, `--apply`, JSON ledger + `--rollback`, `--self-test` 10/10 offline), wired it as step 6 of `check-pinner.sh`, and added the same section filter to `revive-stranded-pins.py` so the next revival cannot re-create the stall. Applied: 2 rows (8007, 8010) had `Board Section ID` cleared so they post to the board root; 51 rows ok, 3 with no section, 0 renamed.

**Before → after:** poster log 2026-09-10 07:00 → 2026-09-12 06:40 CDT: `Posted: 0, Failed: 1, Remaining: 56` on every run; `--check` after apply: `all 56 schedulable row(s) have a live board section`. Posted-count read on 2026-09-13.

**Verdict:** true-positive 🔴, root cause in this repo's own revival script, fixed at Tier 0. Poster-side hardening (retry without `board_section_id` on 404/2031, `id.asc` tiebreaker) is an MHMUtils/scp change → Tier 2 package for Q8.

**Next time:** any script that writes rows a consumer will act on must validate every foreign key the consumer sends to the third party (board id AND section id), not just the URLs. When a batch-size-1, retry-forever consumer stalls, look for the single poison row before looking at tokens or cron. Also: 0 new queue rows since 2026-09-04 — the writer's plugin has not scheduled pins for the last 3 posts; fresh inventory is the next constraint.

## 2026-09-14
- Tried: E46 — second stranded-pin revival (T0: execution of the T1-approved script, PR #69, plus two script changes). `revive-stranded-pins.py --per-day 14 --days 14 --experiment E46 --apply` after `--self-test` (7/7) and a dry run: **196 rows re-dated 2026-09-14 → 09-27 at 14/day**, 28 destinations at exactly 1 pin/destination/day, 18 boards, original dates 2025-01-01..2026-03-05; 0 dead destinations, 1 dead image and 2 dead-section rows dropped before writing. New in the script: destination-host exclusion (default `blog.musthavemods.com`) — 37 rows dropped, **0 blog.* rows in this ledger vs 28/140 in E26** — and an `--experiment` ledger tag. Ledger `reports/funnel/pin-revival-2026-09-14.json`. Sizing rule written before the dry run: 14/day is a third of the ~72/day cron ceiling and below the 43/day observed; 14 days runs 4 days past the 09-23 cliff. Found on the way: `repair-pin-sections.py --check` flagged **1 of 21** schedulable rows on the dead "Black Sims 4 CC" section (E36's) — E26 row 7983, which entered the poster's window only this morning. Added `--days-ahead N`; `--days-ahead 14` found **5** E26 rows (7983, 8024, 7994, 8003, 7968; dated 09-14..09-22) and `--apply` cleared them (`pin-section-repair-2026-09-14.json`, 5 written / 0 skipped / 0 errors). 0 of the 196 new rows were dead. PR #100.
- Before → after: writer plugin **0 rows created since 2026-09-04T11:57Z** (only catalog pins 11428–11430 since) — drought confirmed, not resumed. Schedulable **7 → 21**; future-dated 90 → 286 (24/day 09-15→09-23, 14/day 09-24→09-27); stranded **1,664 → 1,468**; unposted 1,761 unchanged. Pins created 24h 43 on 09-14, all from the 09-04 batch that ran out today — tomorrow without this move: ≤10/day. Pinterest sessions 7d to the 28 destinations **3,579** (GA4 09-06→09-12; 26/28 had traffic). Pinterest 7d total 59,187 (+2.9%).
- Verdict: MORE DATA (read 2026-09-21 cadence — keep if `pinsCreated24h` ≥ 20 every day 09-16→09-21 and step 6 FAILs 0 times; read 2026-09-28 sessions — keep if Pinterest sessions to the 28 destinations ≥ 3,937 (+10%) for 09-20→09-26; rollback `--rollback reports/funnel/pin-revival-2026-09-14.json --apply`)
- Next time: a window-only validator is a race against a batch-size-1 poster — validate the whole horizon a revival writes into, the day you write it. E36's repair on 09-12 passed 51 rows and could not see the 5 dead rows dated 09-13..09-22. Also: 203/1,664 stranded rows (12%) are on `blog.*` — paid-for inventory idle until MHMUtils rewrites the host; and `count=exact` on this table 504s, the same timeout that costs the poster ~1/3 of its slots (Q8).

## 2026-09-13
- Tried: E41 — the scoreboard's 🔴 "Pinner: last posted 2026-09-11 (2d ago)" was a **false positive**. Evidence: poster log shows 47 `Pin posted!` since the 09-12 11:39Z section repair (34 on 09-12, 13 on 09-13 by 10:40Z), 0 failed runs; Pinterest `GET /v5/pins` newest `created_at` = 2026-09-13T10:40:04Z, six minutes before Quinn's 10:46Z check. `n8n_pinterest_posts` has no posted-at column; "last posted" was max(`Post Date`) over posted rows = 09-11 because the rows being drained were created 09-04 with scheduled dates fanned out to 09-11, and the poster takes oldest-first. Shipped (T0, PR today): `scripts/agents/pinner-liveness-lib.ts` (pure, 13 vitest tests incl. the exact 09-13 scenario), `funnel-scoreboard.ts` and `check-pinner.sh` step 1 now read Pinterest `created_at` (🔴/FAIL only if no pin created in 36 h); the Post Date proxy is still printed, labelled "scheduled date", and can only ever 🟡/WARN.
- Before → after: check-pinner step 1 `[FAIL] Pinner stale: last post 2026-09-11 (2 days ago)` → `[OK] last pin created 2026-09-13 10:40Z (0.2 h ago, Pinterest API) · 47 pins in 24h`; scoreboard Flags 🔴 → none; JSON gains `lastPinCreatedAt`, `pinsCreated24h=47`, `pinsCreated7d=108`, `postedTotal=8,066`. Pinterest sessions 7d 59,533 (+2.6% WoW).
- Verdict: **E36 KEEP** (section repair worked: 47 pins posted, 0 dead sections, `Remaining` 56 → 21). **E41 MORE DATA** (read 2026-09-20: 0 false 🔴 across 7 scoreboards while `pinsCreated24h` > 0 every day; kill rule: any 🔴 that the poster log contradicts).
- Next time: a proxy that was right once (09-10 → 09-12, when nothing posted at all) is still a proxy — ask the third party for its own timestamp before a monitor may go red. Two things found on the way, both outside this repo: (a) 25 of ~70 poster runs since the repair lost their slot to Supabase PostgREST **504s** on the poster's `select=*` query, and 2 "Pin posted but failed to mark" events re-posted row 11275 twice — real cadence ≈47/day against 72 capacity; MHMUtils fix = select only the columns it uses + retry the PATCH once (fold into Q8). (b) **0 writer-plugin rows since 2026-09-04 08:36Z** (268 rows created that morning; the only 3 rows since are my catalog pins 11428–11430) — the 09-11 choker-cc post and the 4 others published since have no pins queued, so fresh inventory is the binding constraint by ~2026-09-23 when the E26 revival slice runs out.
