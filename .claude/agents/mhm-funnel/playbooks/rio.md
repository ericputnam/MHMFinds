# Rio — Product & Revenue — Playbook

Your memory across runs. Append one dated entry per run, newest at the top,
**with a number**. "I think it worked" is not a learning. Covers revenue: Patreon tier changes and paid counts, membership conversion, sponsorship replies, affiliate EPC, ad-guardrail incidents.

Entry format:

```
## YYYY-MM-DD
- Tried: …  (tier, PR/link)
- Before → after: <metric> <n> → <n> (<window>)
- Verdict: KEEP / KILL / MORE DATA (read on <date>)
- Next time: one sentence
```

## Kill log
_(ideas you tried that did not work — never re-propose without saying what changed)_

---

## 2026-09-21
- Tried: E74 — `/go` post-connect state (T1, PR #131, QUEUED — merges 2026-09-22 unless "stop 74"; sessions 28d at 96.2% of the line so no T1 merge today). The Connect button's OAuth `callbackUrl` now carries `?patreon=connected` (`withPostConnectMarker` / `hasPostConnectMarker`, pure, in `lib/membership.ts`); when the marker is present and the session is authenticated but not premium, the card shows "Patreon connected, but this account isn't a MustHaveMods member yet. Follow free on Patreon (free-tier join link, id 24870826) — $3/mo patrons skip this wait. Already a patron? Reconnect" instead of the same Connect line. No JWT/session/authOptions change (auth is T2); ad anchors, 10s countdown and both `patreon_click` sources untouched; new GA4 events `patreon_post_connect_view`, `patreon_follow_click`, `patreon_reconnect_click`. 12 new tests (membership 22 → 34), 10 of 12 red against pre-fix `origin/main`.
- Before → after: of Patreon-linked site accounts, in-campaign share (free + paid) **4 of 52 = 7.7%** (48 not in the campaign, 0 active, 0 former; 51/52 created on/after 09-08 ≈ 3.9 linked/day) and paid-and-connected **0 of 54** (E69 pre-read 2026-09-21) → read 2026-10-06 on accounts created 09-22→10-05: keep if in-campaign share ≥ 1/3 (exact fraction) OR paid-and-connected ≥ 1, with session RPM ≥ 95% of the same-weekday 4-wk mean; kill if in-campaign share < 15% AND `patreon_follow_click` < 5.
- E69 (gate reads 09-22), inputs pulled today so tomorrow needs no second query: paid joins since 09-08 **10 in 13.5 d = 22.5/mo (PASS)** · paid-and-connected **0/54 = 0% (FAIL)** · cancels since anchor 1 = 2.3/mo (revert copy: no; a floor until the 10-01 charge run) → **HOLD**. Script exit 0, decision line agrees with the hand rule; API free members 5,409 vs public 5,601 (−3.4%). Not-in-campaign share moved 43/47 → 48/52 (91.5% → 92.3%) in one day — the follow gap is growing, not closing.
- E35 (read 09-17): repeated asks in the digests that exist for 09-11→09-17 (09-12, 09-13, 09-15, 09-16): **0** — the one candidate, `EMAIL_POSTAL_ADDRESS` on 09-12, was still missing that morning (probe 11/13) and appeared 09-13, after which no digest re-asked it. Probe exit 0 on **4 of 4** mornings that fired; 09-11, 09-14, 09-17 had no run at all (runner, not the probe; same shape as the E45 grade). Kill leg (exit 2 on ≥ 3 mornings) not tripped; the keep leg's "≥ 5 of 7" is unmeetable by count when the runner skips 3 days — Quinn's call, my recommendation KEEP.
- Guardrail: GREEN, demand-side — 09-19 $276.15 (+5.7% same-weekday), RPM $19.23 (+5.6%), sessions +0.1%; 3-day revenue +3.4%, 3-day RPM +7.4%; MV health ok; 7d $1,484.80 (−8.0%, sessions-driven), 28d $5,916.48 (+5.4%). Every deploy in the 72h window is docs / collection-page / admin-dashboard code and postdates the judged day. Mediavine MCP unavailable (not in this session's toolset); source: guardrail + scoreboard files. Non-ad $150.50/mo via API ($148 public), 54 paid; 7d joins 3 / cancels 0; patreon_click 3.43 users/day (E65 reads 09-26); affiliate 11 clicks/7d, $0 (E55 reads 09-29). Q9 (PR #17) closes tomorrow by silence — not merged, not touched.
- Verdict: MORE DATA (E74 read 2026-10-06; E69 gate 2026-09-22 from today's numbers unless the connected leg turns overnight).
- Next time: (1) the session carries only `isPremium` — "linked but not a member" is invisible to the client, so any post-OAuth state has to ride the `callbackUrl` (query marker) or a new read endpoint; the marker costs nothing and touches no T2 surface; (2) do not add a third `patreon_click` call site while E65 is open — give a new surface its own event *name* (GA4 `source` is still not a registered dimension, so names are the only queryable split); (3) tier join links are `tier.url` from `GET /campaigns/{id}?include=tiers&fields[tier]=url` — the free tier has one too (rid=24870826), which is the honest first ask for the 92% who never followed; (4) `${PIPESTATUS[0]}` is bash — in zsh it is `$pipestatus[1]`; capture exit codes with a redirect and `$?` instead of guessing.

## 2026-09-20
- Tried: E69 — Q4 gate pre-read (T0). `classifyLinkedAccounts()`, `q4GateDecision()`, `Q4_GATE` constants and `cancelsSinceAnchor` added to the pure `scripts/agents/patreon-members-lib.ts`; entrypoint `scripts/agents/patreon-q4-gate-preread.ts` (Members API `include=user` + `Account`/`User` reads, counts only, `redactError()`, vacuity guard on 0 member rows); 13 new tests (lib file 22 → 35); output `reports/funnel/patreon-q4-gate-preread-2026-09-20.md`.
- Before → after: Q4 gate inputs known 2 days before the 09-22 read — paid joins since 09-08 **10 in 12.5 d = 24.4/mo pace (PASS)** · paid-and-connected **0 of 54 (0%, FAIL)** · cancels since anchor 1 (a floor until the 10-01 charge run) → **HOLD**: no renames, no $10 tier on 09-22 unless the connected leg turns. The *why*, first time measured: of 47 Patreon-linked site accounts, **43 are not in the campaign at all** (never followed, not even free), 4 are free members, 0 active, 0 former. 43/47 are Patreon-only site accounts created by the Connect click itself; 46/47 created on/after 09-08. E40's diagnosis ("free members (5,193) took Connect and got nothing") was wrong on the population: Connect is a *follow* funnel reaching non-followers, and the post-connect state for a signed-in non-member is the same one-line CTA they already clicked.
- E45 grade: **KEEP** — `patreonApi` section `ok` on 4 of 4 mornings that produced a scoreboard (09-15, 09-16, 09-19, 09-20), 0 `unavailable`; 09-14, 09-17, 09-18 have no scoreboard file in either tree (runner failures, not the section). The E40 read was taken from the section on 09-19 as designed.
- Guardrail: GREEN, demand-side — 09-18 $208.00 (+2.3% same-weekday), RPM $17.73 (+7.4%), sessions −4.7%; 3-day revenue +0.6%, 3-day RPM +7.3%; MV health ok; 7d $1,489.72 (−7.1%, sessions-driven), 28d $5,894.82 (+5.3%). Every deploy in the 72h window is docs / collection-page / admin-dashboard code. Mediavine MCP unavailable (not in this session's toolset); source: guardrail + scoreboard files. GA4 `patreon_click` distinct users/day 09-13→09-18: 3, 3, 0, 4, 0, 5 (mean 2.5); **09-19, the E65 revert day: 9** — one day, not a read; E65 reads 09-26. Non-ad $150.50/mo via API ($148 public), 54 paid; affiliate 10 clicks/7d, $0 (E55 reads 09-29).
- Verdict: MORE DATA (re-run the pre-read on 2026-09-22 for the gate itself; keep the tool if it exits 0 and its decision line agrees with the hand rule on the same numbers; re-read the cancels leg after 2026-10-01).
- Next time: (1) the `/go` post-connect state is the product gap — a signed-in, Patreon-linked non-member should see "connected, not a member yet: follow free → $3 skips the wait", not the Connect line again (Tier 0/1 copy on a non-ad surface; `membership.test.ts` guards the anchors) — and the free-follow ask comes first because 91% of connectors are not followers; (2) never infer *who* clicked from counts — classify ids against the member list, the lib now does it in one call; (3) the Members API lists 5,387 free members vs 5,570 on the public page (−3%), so a handful of "not in campaign" may be followers the API omits — far too few to change the ranking, but say so; (4) a leg that compares a rounded share against 1/3 fails at exactly 18 of 54 — compare on the exact ratio, round for printing only.

## 2026-09-16
- Tried: E60 — page-RPM baseline by page type (T0). `scripts/agents/page-rpm-lib.ts` (pure) + `scripts/agents/page-rpm-snapshot.ts` (entrypoint) + 19 unit tests; output `reports/funnel/page-rpm-baseline-2026-09-16.{md,json}`. Mediavine `/reports/pages` pulled per day under two sort orders (revenue, pageviews), de-duplicated on day+path, rolled up into home / collection / mod / go / play / blog, GA4 `screenPageViews` per type as the denominator, one-sided −3% keep floor printed beside every RPM. Aggregates only; every output line and file goes through `redactError()`; 0/2/1 exits.
- Before → after: page types with a dated ad-RPM Before: **0 → 3 measured + 1 bounded** (window 09-08→09-14; site $1,523.74 · 137,215 pv · page RPM **$11.10** · session RPM $17.39). Homepage grid **$10.12** (4,658 pv, 92.3% PV coverage, imp/pv 11.27, viewability 68.0%) · blog articles **$12.97** (136 paths, 60,252 pv, imp/pv 14.07) · collection pages **$7.70** (5 paths, 59.2% coverage — upper bound) · unseen remainder **$9.66** (70,406 pv, $680.28). `/mods/[id]/` (6,375 GA4 pv) and `/go/[modId]/` (342 pv) never enter any day's top-150 under either sort — they are 9.5% of the remainder's pageviews (the rest is the blog long tail) — so their Before is the remainder RPM plus an upper bound of GA4 pv × site page RPM: /mods/ ≤ $70.76/7d, /go/ ≤ $3.80/7d. → read 2026-09-29.
- Finding: the homepage earns 22% less per 1,000 views than a blog article ($10.12 vs $12.97) with the site's *best* viewability (68.0%) but ~3 fewer impressions per view (11.27 vs 14.07) — the gap is slot count, not ad quality. Second finding, the one that changes procedure: **Mediavine's per-path attribution keeps settling after a day's site totals are final.** Two pulls of the same window an hour apart returned identical site totals ($1,523.74 / 137,215 pv) but moved homepage pageviews 4,364 → 4,658 (+6.7%) and homepage RPM $10.58 → $10.12 (−4.3%) — more than the 3% keep tolerance. So the read on 09-29 must re-pull the baseline window in the same session and compare fresh-to-fresh; the figures frozen in the file are a snapshot, not a comparator. **E55's 09-29 keep rule as written ("/mods/ and /go/ page RPM within ±3%") is unmeasurable** — restated: unseen-remainder RPM and site page RPM on 09-22→09-28 each ≥ 97% of the same-session re-pull of 09-08→09-14, one-sided.
- Guardrail: GREEN, sessions-driven — 09-14 $182.03 (−9.3% same-weekday) on RPM $15.58 (+2.3%) and sessions −10.7%; 3-day revenue +6.1%, 3-day RPM +7.8%; MV health ok. Agree with Quinn: no diagnosis. The Mediavine MCP is not in my toolset, but the repo client (`scripts/mcp-mediavine/client.ts`, JWT in the worktree's `.env.local` copy) answered every call across three runs — that is the alternative source my 09-15 note asked for. Non-ad $142/mo public, 52 paid; PR #107 (E55) merged this morning.
- Verdict: MORE DATA (read on 2026-09-29 alongside E55 — run the script twice, 09-08→09-14 and 09-22→09-28; keep the tool if both exit 0 with ≥12/14 pulls and home PV coverage ≥ 80%; fix or kill if the top-150 cap starts hiding the homepage too).
- Next time: (1) `/reports/pages` returns at most 150 rows per query and paging is a no-op (`total_count` is always 150, page 2 empty) — never promise a per-path /mods/ or /go/ RPM and never write a keep rule on one; (2) Mediavine's pageviews equal GA4 `screenPageViews` to the unit (137,215 = 137,215), so GA4 `pagePath` filters are valid absolute denominators — but per-path attribution lags the totals, so a "finalized day" is finalized at the site level only; (3) the grid-card flip is one env var (T2) or one default (T1) and now has its two before-numbers — home RPM $10.12 and imp/pv 11.27 — but the homepage is 3.1% of site revenue, so its ad ceiling is ~$47/wk either way; the $200 non-ad target will not come from the ad side of that page; (4) a raw NUL byte slipped into a template literal as a dedupe separator and only `file`/ripgrep noticed — tsc and vitest were both happy; use a printable separator.

## 2026-09-15
- Tried: E55 — executed the E13 cut-or-kill rule (T1, branch `funnel/rio/affiliate-placements-kill-switch`, merges 2026-09-16 unless stopped). New pure module `lib/affiliatePlacements.ts` with defaults `{ grid: true, mod_page: false, interstitial: false, sidebar: false }` and an exact-allow-list env override `NEXT_PUBLIC_AFFILIATE_PLACEMENTS` (literal `process.env.` read — the E24 lesson — with an injectable value for tests only). Gated at the chokepoint (`AffiliateRecommendations` skips its `/api/affiliates/match` POST and renders null) and at both mount sites (`ModDetailClient`, `GoClient`) so no empty wrapper div is left; `ModGrid` and `useAffiliateOffers` honour the switch but default ON. 19 tests in `__tests__/unit/affiliate-placements.test.ts` incl. source-level guards that `.mv-ads` wrappers, `aside#secondary` and the ModGrid `mv-ads` class are untouched.
- Before → after: affiliate clicks 7d **21** (grid 14, interstitial 4, mod_page 3), 30d **67** (grid 51, interstitial 8, mod_page 8), all-time 1,211 since 2026-01-26; `AffiliateEarning` rows **0**, commissions **$0.00 ever** (DB read 2026-09-15T10:56Z; 88 offers, 6 active). The two blocks switched off carried 16 of 67 clicks/30d and $0 → read 2026-09-29: /mods/[id] and /go page RPM within ±3% of their 09-08→09-14 mean (source: Mediavine daily CSV or guardrail files; MCP unavailable today), affiliate revenue lost $0 because there was none.
- Grid placement deliberately NOT cut: `AffiliateCard` cells are children of the ModGrid `.mv-ads` container on the homepage (top earning page) and /games/[game]; removing up to 4 cells shortens the grid and can shift Mediavine in-content injection, and no homepage page-RPM before-snapshot was obtainable today (Mediavine MCP absent from the toolset). That flip is a separate call: either a Tier 2 draft with a before-snapshot, or the env override once a page-RPM baseline exists.
- Grades: E5 CLOSED/KEEP (weekday RPM < $14 streak 0/5; last sub-$14 weekday 09-01 $13.75; 09-02 $14.21, 09-03 $14.64, 09-07 $18.71, 09-08 $16.79, 09-10 $16.48, 09-11 $17.44; 09-09/09-12 daily source unavailable). E9 CLOSED/KEEP (28d RPM $16.35 > $15, 28d revenue $5,912.54 +3.8% vs prior; mechanical roll-through complete). E13 KILL as a revenue line → E55. E24 KEEP (patreon_click 68/7d, member_skip_countdown 1; 7d RPM $17.83 = +13.8% vs $15.67 — outside the ±5% band *upward*; the band was mis-written as symmetric for a must-not-fall guardrail and the move is demand-side eCPM, not the surface).
- Guardrail: GREEN, demand-side — 09-13 $285.07 (+12.9% same-weekday), RPM $18.58 (+10.2%), sessions +2.7%, 3-day revenue +9.9%, MV health ok; 7d $1,604.88 (+15.0%), 28d $5,912.54 (+3.8%). Every deploy in the 72h window is scripts/reports/feeds code. Mediavine MCP unavailable; source: guardrail + scoreboard files.
- Verdict: MORE DATA (read on 2026-09-29 — keep if earning-page RPM within ±3% and `affiliate_click` GA4 events fall to grid-only; revert = flip `mod_page`/`interstitial` to `true` in `DEFAULT_PLACEMENTS` or set `NEXT_PUBLIC_AFFILIATE_PLACEMENTS="grid,mod_page,interstitial"` + redeploy).
- Next time: (1) write keep-if bands as one-sided when the metric is a must-not-fall guardrail (E24's ±5% produced a "miss" on a +13.8% *gain*); (2) daily weekday RPM lives only in `reports/funnel/guardrail-<date>.md` for days a run fired — 09-09 and 09-12 have no file, so the E5 streak count is 7 of 9 weekdays observed, say so; (3) the Mediavine MCP is not in my toolset — do not plan a move that needs a same-day page-RPM snapshot without an alternative source named first.

## 2026-09-13
- Tried: E45 — Patreon API section on the daily scoreboard (T0, `scripts/agents/patreon-members-lib.ts` pure lib + `pullPatreonApi()` in `funnel-scoreboard.ts`, 9 unit tests). Every morning it now prints paid-and-connected, $3-tier joins since 09-08 and per day, paid joins/cancels 7d, `patreon_click` users/day (GA4 `totalUsers`, new), with the E40 keep/revert rule and the Q4 09-22 gate rule verbatim beneath the numbers — so neither read is hand-run. Aggregates only; emails are used solely to intersect paid patrons with `Account.provider='patreon'` rows and never leave the function (source-level test asserts no `${...email...}` interpolation in the scoreboard).
- E40 BEFORE, frozen on merge day (PR #83 merges today): paid-and-connected **0** of 31 linked · paid **51** (8×$1, 42×$3, 1×$8) ≈ **$141.50/mo** via API ($139 on the public page, which reads the $8 pledge as the $5 tier) · $3-tier joins since 09-08 **4** (7 all tiers in 5d; 7d joins/cancels 8/1) · `patreon_click` users/day **8.8** over 09-08→09-12 (13, 13, 3, 6, 9; GA4 finalized) and 5.0 on the scoreboard's lagged 7d window 09-04→09-11 · `member_skip_countdown` 1 (09-08). Premium-flagged users 2.
- Before → after: hand-run reads needed for the E40/Q4 numbers 2 scripts/morning → 0 (read 2026-09-19 with E40; Q4 gate 2026-09-22).
- Guardrail: GREEN, demand-side — 09-11 $208.83 (+4.1% same-weekday), RPM $17.44 (+7.2%) on sessions −2.9%, 3-day revenue +3.2%, MV health ok; 7d $1,602.85 (+22.8%), 28d $5,816.49 (+1.5%). Every deploy in the 72h window postdates the judged day (all 09-12/13). Mediavine MCP unavailable; source: guardrail + scoreboard JSON. E5 affiliate: 22 clicks/7d, 63/30d, $0 — cut-or-kill read still 09-15, nothing changed.
- Verdict: MORE DATA (read on 2026-09-19 — keep if the 09-14→09-19 scoreboards each carry the section with `ok` status; kill/fix if `patreonApi` reports unavailable on ≥2 of 6 mornings).
- Next time: (1) **set `MHM_PROJECT_DIR` before any local scoreboard run** — the script's dated `.md/.json` writes default to the operator tree, and I created stray untracked `reports/funnel/2026-09-13.{md,json}` there today (documented in the 09-04 compound note; I still tripped it); (2) the GA4 capture window is anchored at `daysAgo(2)`, so "users/day" on the scoreboard lags the MCP read by two days — compare like with like; (3) the public page under-reports gross when a patron pledges above a tier price ($8 shows as $5) — the API line is the one to quote.
## 2026-09-12
- Tried: E40 — /go member CTA leads with the $3 perk-tier checkout, Connect second (T1, PR #83, branch `funnel/rio/go-cta-join-first`, merges 2026-09-13 unless stopped). New constants `PATREON_MEMBER_TIER_CHECKOUT_URL` (`/checkout/MustHaveModsOfficial?rid=24880520`) and `PATREON_MEMBER_TIER_PRICE_LABEL` in `lib/membership.ts`; signed-in non-members get "Not a patron yet? Join for $3/mo … Already a patron (or just joined)? Connect Patreon". GA4 source names, ad anchors and countdown unchanged; membership tests 20 → 22.
- Before → after: paid-and-connected patrons **0 of 27 linked** (linked accounts 1 → 27 between 09-08 and 09-12: 5/10/4/6/1 per day) while `patreon_click` ran 21/20/4/10 events (~35 users) — ~3 of every 4 clicking users connected and none paid, because the first link was "Connect". Paid 50 ($1×8, $3×41, $8×1) ≈ $139/mo; joins since 09-08: 6 in 4 d vs 17/mo baseline; cancels 1. → read 2026-09-19.
- Decision rule (written before the read): keep if paid-and-connected ≥ 3 OR $3-tier joins 09-13→09-19 ≥ 11, with session RPM within ±5% of $17.55; kill/revert if paid-and-connected still 0 AND joins < 8 AND patreon_click users/day < 50% of 8.75.
- Affiliate read (E5/E9, due 09-15) deliberately NOT pulled forward: 63 clicks/30d (grid 51, mod_page 8, interstitial 4), 0 `AffiliateEarning` rows across 1,203 clicks since 2026-01-26, and the Impact sync ran healthy on 09-11 ("0 earnings created"), so the $0 is a real zero — but a cut earns $0 by construction and would have competed with this PR for the one Tier 1 veto window. Rule for 09-15 stays: EPC still $0 → remove the mod_page + interstitial blocks (12 clicks/30d) first; the grid placement (51) is the homepage and needs its own before-snapshot.
- Guardrail: GREEN (09-10 $185.28 +3.5%, RPM $16.48 +9.2%, sessions −5.2%; 3-day +4.4%); every deploy in the window is docs/report code. E5 counter still 0/5. Mediavine MCP unavailable again; figures from guardrail/scoreboard files.
- Verdict: MORE DATA (read on 2026-09-19)
- Next time: (1) GA4's `source` event param is **not a registered custom dimension** — `customEvent:source` 400s in the Data API, so the E24 "by source" split is not queryable; split by date around the deploy instead, and ask for `source` to be registered as an event-scoped custom dimension (GA4 admin, operator); (2) `patreon-relaunch-read.ts` is the one-command read for paid-and-connected — run it, don't re-derive; (3) the `$5` tier's single patron pays $8 (custom amount), so "tier price" and "what patrons pay" differ — report both; (4) tier ids and checkout URLs come from `GET /campaigns/{id}?include=tiers&fields[tier]=url` and are public join links, safe to hardcode.

## 2026-09-10
- Tried: E35 — morning "operator did" probe (T0, `scripts/agents/operator-did-probe.ts` + `operator-did-probe-lib.ts`, runner step 0d, 23 unit tests). Read-only, names/titles/counts only, every output line through `redact()`: (1) Vercel Production env var NAMES via `vercel env ls production --cwd $MHM_OPERATOR_TREE` diffed against the 13 expected names (8 newsletter + 4 `PATREON_*` + membership flag); (2) Patreon tiers via `GET /campaigns/{id}?include=tiers` (title, price, published, patron_count, edited_at, perk-line and welcome-note phrase in `description`, `campaign.thanks_msg`); (3) `check-blog-sidebar.sh` exit code; (4) delta vs `reports/funnel/operator-did-<date>.json`. Exit 0/2/1, one line per run in `logs/operator-did.log`. Baseline 09-09 is a labelled reconstruction from the documented 09-09 observations, not a probe run.
- Before → after: digest asks repeated after the operator had already done the thing: **2** on 09-09 (SMTP vars, $3 perk line) → read 2026-09-17. First real run 2026-09-10 ~11:00Z, exit 0: env **11/13** required present — **+UNSUBSCRIBE_SECRET since 09-09**, still missing `EMAIL_POSTAL_ADDRESS` (hard blocker) and `NEXT_PUBLIC_SITE_URL`; tiers $0 Free 5,330 · **$1 Support Tier still published, 8 patrons** · $3 Tip Jar 40 (perk line present, edited 09-09T01:35:05Z, patrons 39 → 40) · $5 Extra Support 0; `campaign.thanks_msg` empty, no tier description carries the note phrase; blog markers ok. **Q4 step 1 still 1 of 3.** Non-ad $128/mo (48 paid).
- Guardrail: GREEN, demand-side — 09-08 $195.87 (+7.8% same-weekday), RPM $16.79 (+14.9%) on sessions −6.0%, 3-day revenue +27.0%, MV health ok; every deploy in the 72h window is newsletter/report/ingest code with no earning-page change. Source: guardrail JSON + scoreboard (mediavine-reporting MCP failed to connect again). E5 counter 0/5. Affiliate 15 clicks/7d, $0 — cut-or-kill read still 09-15.
- Verdict: MORE DATA (read on 2026-09-17 — keep if 0 repeated asks across the 7 digests 09-11→09-17; kill if the probe exits 2 on ≥3 of 7 mornings).
- Next time: (1) Patreon API v2 has **no per-tier welcome-note field** — the tier attribute list ends at `title/unpublished_at/url/user_limit`; never promise to verify a welcome note, report it "not observable via API" and use `tier.description` + `campaign.thanks_msg` as the proxies; (2) Patreon returns tier titles verbatim with trailing whitespace (`"Tip Jar - Curious Simmer "`) — trim before matching, or a reconstructed baseline reads as "tier removed + new tier"; (3) a reconstructed baseline with `edited_at: null` must compare edits against the baseline *date*, otherwise every tier's May edit reads as "changed today"; (4) a "32+ token chars" redaction regex eats dated filenames unless hyphens/dots are excluded — test `redact()` on a path like `patreon-welcome-note-2026-09-09.md`; (5) the worktrees are not Vercel-linked — `vercel env ls` needs `--cwd <operator tree>`, and `vercel` resets the shell cwd.

## 2026-09-09
- Tried: E30 — Patreon churn read + day-0 welcome-note draft (T0, `scripts/agents/patreon-churn-read.ts`, `reports/funnel/drafts/patreon-welcome-note-2026-09-09.md`). Read-only Members API pull, aggregates only (no emails/tokens printed).
- Before → after: 47 paid ($1×8, $3×39) $125/mo · 225 former · 17 declined · 5,116 free. Cancel cohort by charges paid (n=223): 1 → 88 (39%), 2 → 44 (20%), 3 → 33 (15%), 4–6 → 34, 7–12 → 17, 13+ → 7; ≤2 charges = 132 (59%), median 2. First-30-day churn 84/250 = 34%. Joins vs cancels Jun–Aug 56/29 (18.7 vs 9.7 per month); Sep to date 5/16. Site linkage: 11 `Account.provider='patreon'` rows (5 on 09-08), **0** belong to a currently paying patron, `isPremium`=2. E24 interim: `patreon_click` 27 events (~20 users, all on `/go/`), `member_skip_countdown` 1.
- Q4 step 1 via API: $3 tier description edited 2026-09-09T01:35Z (perk line applied, 6 min after PR #65 merged); $1 tier still `published=true` with 8 patrons; newest post is 09-08 "Y2K Belts Lookbook" — thank-you post not posted. 1 of 3 steps done.
- Guardrail: GREEN is demand-side. Source: repo Mediavine client (`earnings`), MCP unavailable. Daily eCPM $0.70–0.85 (08-31→09-04) → $0.91/$0.92/$0.95 (09-05/06/07) while paid imp/pageview stayed 11.1–13.1 and viewability 51.5–53.9% — price per impression moved, fill and layout did not. Same-path page RPM up on 100/109 paths 09-05→09-07 vs prior week (homepage $7.29→$15.12, not finalized). The +34.5% RPM read is against a 4-Monday base that includes the 08-31 $11.96 trough; every 09-08 deploy postdates the judged day. E5 counter 0/5 (09-01 $13.75 was the only sub-$14 weekday; 09-02→09-07 all ≥ $14.21).
- Verdict: MORE DATA (read on 2026-10-07 — keep the welcome note if post-note first-30-day churn ≤ 24%, or ≥ 1/3 of new paid patrons connect on-site within 7 days; kill by 2026-10-22 if neither).
- Next time: (1) attribute a green to demand only after checking eCPM vs imp/pv vs viewability separately — a layout change moves imp/pv, a demand change moves eCPM; (2) Mediavine `pages()` keys are `page_revenue`/`rpm`/`pageviews`/`cpm`/`impressions_per_page_view`, and the JWT lives in `scripts/mcp-mediavine/.env.local`; (3) a decision threshold set at 40% when the numbers land at 39%/34% is a threshold chosen after the fact — pick the rule from the question ("where is the leak") before the read, not from the first printout; (4) the Patreon posts endpoint paginates oldest-first, sort client-side.

## 2026-09-08
- Tried: Q5 end-to-end verification found the approved membership feature dark for visitors and fixed it forward (T1, PR #62, `032543e`, merged same day as a fix-forward on an approved Tier 2 package — Quinn flagged the skipped 24h veto in the digest). `isMembershipEnabled()` read the flag as `env[MEMBERSHIP_FLAG]`, which Next.js never inlines into client bundles, so the browser evaluated `undefined` for ~22h while `/api/auth/providers` listed `patreon` the whole time.
- Before → after: `/go` CTA / Connect button / patron link rendered on production: false → true (headless render 07:09, served chunk `page-c26189679e4c0033.js`); `patreon_click` / `member_skip_countdown` / Patreon-linked accounts 0 / 0 / 0 (09-06→09-08) → read 2026-09-15. Non-ad $126/mo gross (46 paid: $1×6, $3×40; Q4 ladder still not applied in Patreon). Homepage MV page RPM $9.88 (08-31→09-06) vs $8.73 prior 7d — the E18 read baseline. `/go` per-page RPM: source unavailable (Mediavine `/reports/pages` caps at the top 150 paths); ceiling by GA4 236 pv × $15.67/1000 ≈ $3.70/7d.
- Verdict: MORE DATA (read on 2026-09-15 — keep if ≥1 `patreon_click` on /go and session RPM within ±5% of $15.67)
- Next time: (1) any `NEXT_PUBLIC_*` flag must be read as a literal `process.env.NEXT_PUBLIC_X` on the client path — guard it with a source-level test; (2) day-1 verification of a client-facing feature means a logged-out headless render of production plus grepping the served chunk for the inlined value, not a curl of a server route; (3) when the ad-revenue read for a page is impossible, say "source unavailable" and use GA4 pageviews × site RPM as an explicit upper bound; (4) ESM scripts resolve `node_modules` from the script's directory, not `NODE_PATH` — run Playwright helpers from inside the worktree. E5 escalation counter 0/5 (09-03 $14.64, 09-04 ≈$16.30, 09-05 $18.05). Affiliate cut-or-kill read still 2026-09-15 (12 clicks/7d, $0 EPC, 56/30d).

## 2026-09-07
- Tried: membership via Patreon OAuth as a ready-to-approve T2 package (PR #52, branch funnel/rio/membership-patreon-oauth, not merged): Patreon provider in NextAuth + /go countdown skip + member badge, all behind NEXT_PUBLIC_MEMBERSHIP_ENABLED; pricing knob PATREON_MEMBER_MIN_CENTS. Package: reports/funnel/drafts/membership-patreon-oauth-2026-09-07.md → operator-queue Q5.
- Before → after: non-ad $127/mo gross (47 paid: $1×7, $3×40, $5×0; 5,268 free) → target $200/mo by 09-30 (Q4+Q5: relaunch A targets 90 patrons ≈ $475/mo). Ad-loss bound: /go/ 725 GA4 pageviews/28d × $15.60 = ≤ $11.31/mo for the whole page.
- Guardrail, first green since 09-02: 09-05 $269.49 (+12.0% same-weekday), RPM $18.05 (+1.8%), sessions +10.1%, 3-day +1.6%. 28d $5,628.24 (−7.6%) is the E9/E13 mechanical roll-through and is improving: −10.4% (09-04) → −9.7% (09-05) → −7.6% (09-07). E5 counter 0/5 (weekday RPM 09-03 $14.64, 09-04 ≈ $16.30 derived from the guardrail 3-day window, 09-05 $18.05). Mediavine MCP unavailable; figures from guardrail/scoreboard JSON + GA4.
- Finding: 0 Google/Discord-linked accounts ever (all 1,533 are credentials) — signIn pre-create + no allowDangerousEmailAccountLinking → OAuthAccountNotLinked. Separate T2.
- Verdict: MORE DATA (read on 2026-10-07, 30 days after the flag flips).
- Next time: pitch Q4 and Q5 as one decision.

## 2026-09-01
- Tried: nothing yet — team chartered today. Read `../charter.md`, `../autonomy.md`, `../operating-model.md`, `../targets.json`, and `reports/growth/fact-base-2026-09-01.md` before your first move.
- Before → after: baseline in `../targets.json`
- Verdict: —
- Next time: your first move should be the top item in your agent file's "levers" list unless the scoreboard shows a 🔴 in your area.
