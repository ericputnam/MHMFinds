# Nova — Content & Creators — Playbook

Your memory across runs. Append one dated entry per run, newest at the top,
**with a number**. "I think it worked" is not a learning. Covers content: which briefs the writer used and how they performed, collection pages that worked, creator outreach response rates.

Entry format:

```
## YYYY-MM-DD
- Tried: …  (tier, PR/link)
- Before → after: <metric> <n> → <n> (<window>)
- Verdict: KEEP / KILL / MORE DATA (read on <date>)
- Next time: one sentence
```

## Kill log
_(ideas you tried that did not work — never re-propose without saying what changed)_

---

## 2026-09-21 (triage)
- Tried: triage of two 2026-09-01 operator ideas that sat unactioned for 20 days (T0 docs, PR #TBD, E79/E80): "host creators' mods directly" and "gaming catalog beyond Sims 4". Read `app/api/submit-mod/route.ts` in full to confirm it never writes `downloadUrl`/`sourceUrl` (URL-pointer only, no storage/upload path exists) before recommending anything on hosting. Queried the DB directly for real candidate creators and submission counts rather than guessing, and tried to size off-site audience via WebSearch/WebFetch before concluding Patreon/Tumblr don't expose public follower counts anymore — said so plainly instead of estimating. For the second game, contrasted GA4 session volume against Pinterest/RPM content-shape fit and competitor saturation (CurseForge 226K Minecraft projects vs Nexus 33,828 Stardew mods) rather than picking the higher-traffic option by default.
- Before → after: two decision memos written (`reports/funnel/triage/2026-09-21-creator-hosting.md`, `2026-09-21-second-game.md`), Q14/Q15 opened in operator-queue.md, ideas-inbox.md status lines added, E79/E80 queued rows added. Before: both ideas unread since 2026-09-01, zero non-Sims-4 collection pages, zero creator-facing submissions ever used.
- Verdict: MORE DATA — both are Tier 2 proposals awaiting operator reply (Q14, Q15); read dates 2026-10-19 and 2026-10-05 respectively, conditional on approval.
- Next time: when a task asks for "real numbers, not opinions" on something the schema *looks* ready for (creator hosting), read the actual route handler before trusting the model — the schema having a `downloadUrl` field says nothing about whether any code path populates it. Recommending the smaller, already-buildable slice (profile pages + wiring existing fields) instead of the literally-requested but riskier feature (file hosting) is a legitimate Tier 2 answer when the risk (DMCA, malware) isn't yours to accept unilaterally.

## 2026-09-21
- Tried: `kids-cc` collection page **and the class bug behind it, in one PR** (T0, PR #136, `7816cbe`, E72). On 09-20 I rejected this cluster — 752 rows on infant/toddler/child, only 45.6% of titles carrying a kid word — and wrote that it was "gated on an ageGroups repair, not a page". It was. `extractFacetsFromKeywords()` in `lib/services/aiFacetExtractor.ts` matched a keyword map with a bare `text.includes(keyword)` over `title + description + tags`, and two entries were substring poison: `'ya' -> young-adult` fires on Ra**ya**n / To**nya** / ro**ya**l, `'tot' -> toddler` on **tot**al / pho**tot**. `'baby' -> infant` fired on the aesthetic use. The description half is the same contamination that was cleaned out of `lighting` (09-08), `gameplay-mod` (09-10), `jewelry` (09-18) and `nails` (09-20) — **this is the first time it was fixed at the source instead of swept out of one facet**. New `lib/ageGroupRules.ts` is the single source of truth, imported by the extractor, by the new `scripts/retag-age-groups.ts` and by the new test, so `scripts/deploy-facets-safely.ts` cannot re-poison the column. The bug was also **bidirectional**: 363 rows that say so in the title (*Functional Infant Cribs* 473 dl, *Toddler Traits Pack* 372, *Skin CC for Toddlers & Infants* 341) carried no kid tag at all, which no facet-cleaning pass would ever have found.
- Before → after: `ageGroups` kid axis **752 rows / 343 title-supported (45.6%) → 686 / 686 (100%)** — 362 added, 428 stripped, 35 rewritten, 289 no-ops over a 1,114-row union population, verified by a separate read of the changed field (toddler 362, child 266, infant 81). Collection routes **22 → 23**, edges 66 → 69. `/games/sims-4/kids-cc/` live 200 with `.mv-ads`=1, `aside#secondary`=1, CollectionPage JSON-LD, in `sitemap-nextjs.xml` and `llms.txt`; 686 mod detail pages gain a second collection breadcrumb and none lose their first (`ageGroupsAny` scores 1 in `filterSpecificity()` and the entry is appended last). Baselines for the read: page 0 sessions (did not exist); collection pages in aggregate GA4 09-13→09-19 **1,069 engaged / 1,322 sessions across 22 pages**; GSC 28d to 09-18 for the legacy cluster 23 articles / ~1,836 impressions / 12 clicks at pos 21.9–52.3, `/sims-4-kids-cc/` itself 153 imp / 0 clicks / pos 43.3. Also read **E3's brief leg (09-09, empty since)**: **0 of 5 W36 topics adopted, 0/1 briefs** — blog.musthavemods.com WP REST, posts 09-02→09-09 were nicki-minaj-cc, social-media-mods, cardi-b-cc, goth-accessories, goth-nails-cc against a brief of witch / makeup-eye-looks / decor / accessories / eyes. Still 0 as of 09-21.
- Verdict: MORE DATA (read 2026-10-19; keep if ≥15 engaged sessions in the 7d to the read date **and** aggregate collection-page engaged sessions ≥95% of 1,069 **and** `/sims-4-kids-cc/` impressions ≥80% of 153 — i.e. the page has to add, not cannibalise). E3 brief leg reads **KILL on the brief format, not on briefs**.
- Next time: three things. **(1) Distinguish a class bug from heterogeneous junk by asking whether one rule is uniformly wrong, and check both directions.** 09-20's nails case was six wrong rows for six reasons and correctly got `--ids=`; this was one `includes()` with no word boundary and correctly got the class tool. The tell is the *missing* rows: a facet that is 45.6% clean **and** has 363 obvious titles with no tag at all cannot be explained by a hundred independent mistakes. **(2) The spot-check rule paid twice in one run.** The first simulation would have made *Baby Face Kit* (2,878 downloads — an adult **lips preset**) card #1; the second would have made *Child Birth Mod* (1,383) card #1. Both are now encoded rather than merely noticed — `baby` is a rejected keyword **with its count** (71 titles) in the constant's own comment, and `AGE_NEGATIVE_TITLE_CONTEXTS` covers child birth / birth control / baby shower / try for baby. **(3) `deploy-verify.sh --after-merge --sha <sha>` exited PASS against a build that did not contain the sha.** It waited, saw `epekaola2` go READY, promoted it and graded it green — but `/games/sims-4/kids-cc/` was **404** on the apex at 07:15 with `x-matched-path: /games/[game]/[topic]`, i.e. the route ran and `getCollection()` returned null, and `/llms.txt` carried #134 but not #136. The page went 200 at 07:17 on the *next* build. So the sha wait matches a deployment by timing, not by commit, and four merges inside five minutes (#132 11:05:00, mine 11:09:47) is enough to break it even when the 4-minute serialization rule is honoured. **A new page is not shipped until you curl the page itself** — deploy-verify's smoke set does not include collection pages, which is why 22 pages have shipped behind a check that never looked at one. A second ledger row (07:17 recheck) records the discrepancy. Next T0 candidates: add a `collection` kind to `smoke-render.ts` so the newest registry entry is always fetched, and grade E3 by rewriting the brief format — the writer's actual 20 posts/month are celebrity CC (nicki-minaj, cardi-b), aesthetic clusters (y2k, goth) and **single-item** clusters (choker, candle, boat, cardigan, nails), while every brief I have written ranks broad *category* head terms by GSC impressions. 0/15 adoption across W36–W38 is a brief-shape failure, not a writer failure.

## 2026-09-20
- Tried: `nails-cc` collection page + a 6-row repair of the `nails` facet in the same PR (T0, PR #127, E67). `nails` was the last clean un-paged contentType: 151 rows, 0 pages, 0 mod detail pages with a collection breadcrumb. **Repaired first, shipped second** — the 09-07 rule ("spot-check the top rows before a facet backs a landing page") caught a feet body mod as card #2. Six rows moved by hand audit, each read against its own description (Feet 1V Remaster -> body-preset, Sims 4 Wedge Sandals -> shoes, Mediterranean Sunrise + E&R Gilmore's Villa -> lot, Bruna Ring Set + WM Rings 202001 -> jewelry). Five *real* nail sets whose titles name no nail (Black Tips in 5 Shapes, Brina B Set, Sponge Bob Summer Set, Ashley Set, Default Pedicure) pinned to `nails` in `hand-audited-content-types.ts` so a future facet-wide run cannot clear them — the Green Lantern failure mode from 09-10. `retag-junk-build-facets.ts` gained `--ids=` to make the narrow fix expressible at all.
- Before → after: `nails` facet 151 rows / 92.7% title-clean → 145 / **97.2%** (141 of 145), top 12 by downloads 12/12 genuine and two 8-row mid-grid samples 16/16; collection routes 21 → 22; mod detail pages with a collection breadcrumb +145 (all first-ever); `/games/sims-4/nails-cc/` live and verified 200 with `.mv-ads`=1, `aside#secondary`=1, 4 JSON-LD blocks, in `sitemap-nextjs.xml` (23 collection URLs) and in `llms.txt`. Sessions/favorites baseline 0 (route did not exist, 2026-09-20). Also graded **E38**: `/play/` 09-13→09-19 = **11 sessions, 9 engaged, 12 pageviews** against a keep rule of ≥250 sessions AND ≥15 download clicks — fails by ~23×, so the daily game is not a traffic driver without a distribution plan. jewelry-cc (#118) first 48h: 5 sessions / 4 engaged, pre-indexing.
- Verdict: MORE DATA (read 2026-10-18; keep if ≥200 engaged sessions OR ≥5 favorites in the first 28 days — the bar used for decor-cc, shoes-cc, loading-screens and jewelry-cc). E38 reads **KILL** on its own rule; `/play` keeps its runtime checks (free) but earns no further content work until Pip has a distribution surface for it.
- Next time: **take the facet-wide dry run even when you intend a narrow fix, then throw it away.** `--facets=nails` proposed 21 changes and **15 were wrong** — rule priority beats the literal word "nails" in a title ("S-Club Nails Art Accessories" -> accessories, "Nails N1 - Solid + Chipped Overlay" -> skin, "Sims 4 Toenail Recolors" -> makeup, "Sponge Bob Summer Set" -> hair on "bob"). That is a real detector finding (worth its own PR with a whole-catalog before/after) and would have been an outage on the new page. Six wrong rows for six different reasons is not a class bug; do not reach for the class tool. Second: **a new page's `related` wiring is now degree-constrained** — #120 (Sage) fixed outbound at exactly 3 and inbound at 2–8, so appending a slug turns the suite red; wire a new page in by *repointing* an edge that led to one of the 7-inbound hubs (`skin-details`, `hair-cc`), which is what those hubs' surplus is for. My own per-page inbound test was deleted in the same rebase: #120 ships a registry scanner, and a scanner beats a hand-written per-page assertion. Third: candidates rejected **with their counts, in the filter's own comment** so nobody re-proposes them — gameplay-mod+script-mod+career (511 rows, ad-policy risk), kids/toddler/infant via `ageGroupsAny` (752 rows and the best demand left at 1,795 GSC impressions, but 45.9% clean and the age tags are blanket-applied), `hats` (202) 51.5%, `workout`+`gym` (194) 42.8%, keyword `claw` (12 titles, 4 nails). The kids cluster is the biggest prize on the board and is gated on an **ageGroups repair**, not a page — that is the next T0 worth doing.

## 2026-09-16
- Tried: put `/play/` inside the runtime checks it had been outside of since it shipped (T0, PR TBD, E58) — and fixed the **class**, not the instance. Three edits: (1) `scripts/agents/smoke-render.ts` gains a `game` kind (folded into `adPage`, so it gets the full loader + `aside#secondary` + `.mv-ads` + 400-char text assertions, not a weaker variant) and the target `{ path: '/play/', kind: 'game' }` — trailing slash load-bearing, the bare form 308s and the check would grade the redirect; (2) `sidebar-sticky-health.test.ts`'s three hand-copied page arrays collapsed into one `PAGES_WITH_SIDEBAR` const that sections 1/3/5 all iterate, with `/play` registered; (3) a new **registry-completeness scan** that walks `app/` + `components/` for any `.tsx` declaring `id="secondary"` and fails if it is not in the registry, plus a vacuity guard (`found.length >= registry.length`) so the walk cannot pass by finding nothing. Verified `/play/` against production with the real smoke expectations before writing the target: **200, secondary=1, mv-ads=1, loader present, 2,846 chars, 0 page errors**.
- Before → after: deploy-verify smoke targets **12 → 13**; pages covered by the central sidebar rules **4 → 6**; suites 50→51 files, 894 → **957 tests** (sidebar 27 → 48, play-page 23 → 26), `type-check` and `npm run build` clean. The completeness scan paid for itself on its first run: it found **`app/games/[game]/[topic]/CollectionPageClient.tsx` — the component behind all 20 collection pages, my own page set — had never been in the registry either**, so none of the min-h/sticky/breakpoint rules had ever looked at it. Also found that two of the three sections located the aside on *raw* source, so on CollectionPageClient they were measuring a JSX comment 26 lines above the real element (its capture-surface comment names `<aside id="secondary">`); `stripComments()` now runs first, which is what made `app/page.tsx` correctly drop out (its header JSDoc names the aside; the element lives in HomePageClient).
- Verdict: MORE DATA (read on the next `deploy-verify.sh --after-merge` line naming `/play/`; keep if the smoke run reports 13/13 pass with `/play/` green, kill the target only if it produces a flaky failure that does not reproduce on a second render).
- Next time: **a per-page test is not coverage — it is a second copy of the rules that the registry cannot see.** E38 re-implemented the sidebar assertions inside `play-page.test.ts` rather than registering the file, which reads as thorough and is exactly how `/play` stayed outside the suite; the same omission had quietly been true of the collection pages for far longer. When a rule already has a central registry, the move is to add a row, and the durable fix is a scanner that makes an unregistered file fail. E28 graded KEEP (numbers in the run report): daily ingest is now 1:1 with the blog — 09-10 travel→10 mods same day, 09-11 choker→25 on 09-12, 09-14 boat→19 same day, 09-15 candle→28 on 09-16; 4 of 4 posts ingested, 0 missed, +82 mods in 7d excluding the 09-09 backfill, catalog 16,456.

## 2026-09-15
- Tried: closed the **slashless-href class** (T0, PR TBD, E53) — not one instance, the whole class. `trailingSlash: true` 308s every bare internal href, and the repo had **61** live: the 5 template literals already catalogued (two of them in the site-wide `<Navbar>` "Browse by Game" dropdown, i.e. on every page of the site; the collection-page breadcrumb; `/play`'s "shop the look"; admin edit) **plus 56 plain-string hrefs** nobody had counted — 9 more in Navbar, 8 in Footer (which carries the Sims 4 / Minecraft / Stardew collection links), 4 in `/creators`, and the rest in admin. Verified 11 of them live against production first: `/games/sims-4`, `/go/<id>`, `/blog`, `/creators`, `/sign-in`, `/about`, `/terms`, `/privacy-policy`, `/top-creators`, `/submit-mod`, `/sign-in?mode=signin` — **all 308**, and the query-string case redirects to `/sign-in/?mode=signin`, so the fix has to slash the path portion and keep the query. Guard is a repo-wide source scan in `canonical-trailing-slash.test.ts`: walk every `.tsx` under `app/` and `components/`, strip comments first (house rule — this repo's comments deliberately contain the patterns they warn about), match `href="..."` / `href={\`...\`}` / `href={'...'}`, and require the path before `?`/`#` to end in `/`, with exactly two exemptions (path is bare `/` for root-with-query like `/?search=${tag}`, and a dotted last segment like `/feeds/mods.json`, matching `isCanonicalUrl()` in `indexnow-lib.ts`). Negative-controlled it: reverting one Navbar href fails the suite with `components/Navbar.tsx:109 href=/games/${slug}`.
- Before → after: slashless internal hrefs in `app/` + `components/` **61 → 0**; unit suite 894 → 896 tests, 50/50 files green; `type-check` clean, `npm run build` clean. GSC baseline for the page set the Navbar and Footer now link canonically, 28d to 2026-09-11: `/games/*` = **128 clicks / 7,350 impressions**, positions 21.3–58.4, and the game hub `/games/sims-4/` itself is **115 impressions / 1 click at position 50.4** — the exact URL every "Browse by Game" click was 308ing into. Catalog 16,428 mods; blog 6 posts/7d, latest 09-15 `sims-4-candle-cc`; W36+W37 brief adoption still 0/10 (no brief written today, per Quinn).
- Verdict: MORE DATA (read 2026-10-13; keep if `/games/*` GSC impressions are flat-or-up and no new slashless href has landed — the guard makes the second half self-enforcing).
- Next time: **four existing tests were pinning the bug in place.** `play-page.test.ts:188`, `Navbar.test.tsx:197` and `:301`, and `password-reset.test.ts:388` all asserted the *slashless* form, so the class could not be closed without editing them — a guard written against a copy of the current value rather than against the rule. Each is now `toMatch(canonical)` **plus** `not.toMatch(slashless)`, which is the shape that cannot re-pin a regression. Also worth recording for the next agent who reaches for a collection page: I rejected candidate (a) on the house spot-check rule. The builds cluster `residential`(591)+`lot`(309)+`builds`(18)+`commercial`(32) = **950** looks like the biggest prize left but is junk-tagged worse than `hats` was — top `residential` rows include a literal garbage title ("Pinterest / Email / Other Apps"), a clothing pack, an infant swing and "Adult Jobs for Teens"; top `lot` rows include "Male Clothing CC Dump" and "Date Night CC Clothing Pack"; mid-grid at skip 600 is "S-Club WM Hat", "Edgy Sims 4 CC Heels", "Lenovo IdeaPad 3 Gaming Laptop". Do not page it until it gets the title-only re-tag treatment `lighting` got. `nails` (149) **does** pass: only 12/149 titles lack a nail word and only ~7 are true junk (E&R Gilmore's Villa, Sims 4 Wedge Sandals, Bruna Ring Set, WM Rings 202001, Feet 1V Remaster) — ready to ship as a page the moment PR #102 is off `lib/collections.ts`, which is the only reason it did not ship today.

## 2026-09-13
- Tried: `shoes-cc` collection page (T0, PR TBD, E43) — the largest **clean** un-paged contentType left in the 16,409-mod catalog. Facet audit of every uncovered cluster first: `accessories` (863) + `jewelry` (550) + `hats` (201) + `glasses` (110) + `watches` (24) is bigger at 1,748, but three of those five facets are junk-tagged the same way `lighting` was — the top `hats` rows are a Coach bag, two hairstyles and a kitchen set; the top `glasses` rows are a bathroom, kitchen appliances, walls and bedding; the top `accessories` rows are "100 Base Game Traits", "SimDa Dating App" and "Slice of Life", all gameplay mods. `shoes` (633) spot-checked 15/15 real footwear on the top-downloads page and 15/15 on a mid-grid sample, so it is the one that can back a landing page today. Also placed the entry **before** male-clothes/female-clothes (both composite `contentTypeIn` filters that already include `shoes`, all three scoring 0 in `filterSpecificity()`, so registry order is the only tie-break) and fixed the related-collections strip, which had been linking `/games/${gameSlug}/${slug}` with no trailing slash — a 308 on every related link on all 18 collection pages since the strip shipped.
- Before → after: collection routes 18 → 19; `/games/sims-4/shoes-cc/` engaged sessions 0 (page did not exist) → read 2026-10-11. GSC baseline for the shoe cluster, 28d to 2026-09-10: 11 blog articles, **1,651 impressions / 19 clicks**, every one at position 16.9–55.4 (`/sims-4-shoes-cc/` 445 imp / 1 click / pos 37.7; `/sims-4-heels-cc/` 420 / 10 / 16.9; `/sims-4-cc-shoes/` 219 / 0 / 55.4). Query side: `sims 4 heels` 16 imp pos 15.1, `high heels sims 4` 7 imp pos 8.9, `heels sims 4 cc` 6 imp pos 12, all with 0–2 clicks. Registry composition, verified against prod 2026-09-13: 137 boots, 71 heels, 66 sneakers, 47 sandals, 86 kid-tagged; top creators Mermalade 41, Madlen 37, Arltos 28, Jius-sims 22+12, LVNDRCC 15. Internal links: 633 mod detail pages now carry a "Shoes CC" primary breadcrumb instead of "Female Clothes CC"/"Male Clothes CC". Related-strip hrefs 0/18 → 18/18 canonical. canonical-trailing-slash suite 18 → 21 tests, green. Blog: 5 posts/7d, latest 09-11 `sims-4-choker-cc`; W37 brief adoption still 0/5.
- Verdict: MORE DATA (read 2026-10-11; keep if ≥200 engaged sessions or ≥5 favorites from the page in the first 28d, same bar as decor-cc).
- Next time: **audit the facet before you rank clusters by size.** Accessories was the obvious pick on row count and would have shipped a page whose top three cards are a dating-app mod, a traits mod and a Coach handbag. The 09-08 lesson (spot-check top rows, not just `expectedCount`) now applies to *choosing* the page, not only to writing the filter. Two follow-ups worth a T0 each: (1) `accessories`/`hats`/`glasses` need the same title-only re-tag treatment `lighting` got — the pollution looks like the same description-inference failure mode, and fixing it unlocks a 1,700-mod accessories page; (2) `nails` (149, clean, all genuinely nails) and `residential`+`lot`+`builds`+`commercial` (943) are the next two clean clusters with no page. Also found: three tests are red on `origin/main` (`llms-txt.test.ts` and 3 in `seo-phase1.test.ts`) because PR #63 un-consolidated pregnancy/y2k but those two suites still assert the old redirects — same "red test sits on main for days" pattern as 2026-09-07.

## 2026-09-10
- Tried: `gameplay-mod` detector rule + NULL re-tag (T0, PR #79, E33) — the follow-up I specced on 09-09. The rule already existed (467 rows); what it lacked were nouns for the three things a gameplay mod is usually named after. Added `career` (56 catalog titles, 25 already gameplay-mod), `trait` (54/35), `overhaul` (21/16), `aspiration` (7/4), `map replacement` (19), `life mod` (5), `side hustle`, `social bunny`. Removed three things by the 09-08 house rules: the bare adjective `realistic` (32 titles spanning beards, body presets, skins, shorts and houses — and because gameplay-mod outranks `lot`, it was actively stealing "Suburban Realistic Houses"), the duplicate `pregnancy` (the priority-103 rule always wins on that word, so it could only ever add a spurious second description match), and four compound spellings that each contain another keyword in the same rule. New `scripts/retag-null-content-types.ts`: title only, dry-run default, 5,000-row cap, `where: { contentType: null }` so no tagged row can be touched.
- Before → after: catalog rows carrying a contentType facet 15,929/16,384 (97.22%) → 15,994/16,384 (97.62%); NULL 455 → 390; gameplay-mod 467 → 480, decor 611 → 633 (22 wallpapers, which is also 22 more cards on the decor-cc page I shipped 09-07), full-body 1,468 → 1,486, furniture 984 → 989, lot 300 → 302; detector tests 21 → 30. Whole-catalog diff, title-only, old vs new detector: 114 rows gain a gameplay-mod answer they did not have, 12 lose one (all `realistic`), 9 move lot → gameplay-mod (8 of them are genuine lot *traits*), 1 moves gameplay-mod → lot. Blog: 6 posts/7d, latest 09-10 `sims-4-travel-mods`; W37 brief adoption still 0/5.
- Verdict: MORE DATA (read 2026-09-24; keep if the /games/* grids are flat-or-up, a spot-check of the 65 re-tagged rows is still 100% correct, and the next ingest run adds < 10 NULL rows from a "mods" post).
- Next time: **run the new script's dry run against the previous audit before trusting it.** The title-only pass proposed re-tagging "Green Lantern – Injustice God Among Us" as `lighting` — the exact row PR #61 had hand-cleared two days earlier — because the seven hand-audited ids lived as a private `OVERRIDES` const inside `retag-junk-build-facets.ts`. Any human judgement a script encodes has to live somewhere every script can import (now `scripts/lib/hand-audited-content-types.ts`), or the next cleanup silently undoes the last one. Second finding, generalised into the detector: a rule listing both a compound keyword and a word it contains ('social interaction' + 'interaction') double-counts exactly like 'light' + 'lights' did, and stem-collapsing does not catch it — `matchedKeywordsIn` now drops any match contained in a longer match. Third: 390 rows are still NULL and title-only cannot fix them (proper-noun CC like "Neverland Fairy", "3 Lunea – Part 2"); the only honest signal left is the source post slug (14 plumbob replacements, 16 phone replacements), which is a post-level inference pass, not a detector rule — next T0 if the facet-coverage number matters more than the risk.

## 2026-09-09
- Tried: catalog-ingest diagnosis + fix (T0, PR #73, E28). Cass's "0 mods in 7 days" was true and understated: last insert 2026-08-09 22:23Z, `scraping_jobs` has 0 rows ever, no launchd/cron/runner step calls any scraper, and every non-blog source (Patreon/Tumblr/TSR/CurseForge) last inserted 2026-01-29. Nothing broke — ingest was never scheduled; `scrape:mhm` runs only by hand and keeps freshness in an untracked `data/mhm-scraped-urls.csv` in the operator's checkout, so no worktree or job could run it without re-crawling all 667 posts. Added `--new-only` (freshness = distinct `Mod.sourceUrl` in the DB), `--since` (sitemap `<lastmod>`), `--dry-run`, a `scripts/agents/catalog-ingest-daily.sh` wrapper that logs one line per run, and 12 unit tests; ran dry (22 posts, 496 would-create, 0 errors), then live.
- Before → after: mods 15,888 → 16,374 (+486 from 22 posts published 08-10→09-09; 1 updated, 19 dedupe skips, 0 errors); posts with a mod row 493 → 515; wrapper re-run selects 0 of 668 (idempotent). 81 of 486 new rows have NULL contentType — all from the four "mods" posts (social-media, phone, funeral, moving): the detector has no rules for careers/aspirations/traits. Blog: 6 posts/7d; W37 adoption 0/5 (09-09 `sims-4-goth-nails-cc` is not a brief topic; goth cluster is the writer's own run).
- Verdict: KEEP the flags (they cost nothing when there is nothing to do). MORE DATA on cadence: read 2026-09-16 — keep if `mods added 7d` > 0 on every day a post was published, i.e. the schedule Quinn picks actually fires.
- Next time: when a pipeline "stalls", check whether it ever had a schedule before looking for a break — the whole insert history was manual bursts. Any state a scheduled job needs must live in the DB or in git, never in an untracked file in one checkout. Detector follow-up: a `gameplay-mod` rule for career/aspiration/trait/mod-title patterns would have tagged ~70 of the 81 NULLs.

## 2026-09-08
- Tried: junk-facet repair (T0, PR #61) — fixed `lighting`/`curtains` at the detector level, not just in the data. Two bugs: `keywordToRegex` already appends `(?:s|es)?`, so a rule listing both 'light' and 'lights' scored TWO matches off the single word "lights" and cleared the ">=2 description matches = medium confidence" bar; and the bare adjective 'light' was a `lighting` keyword. Then re-tagged the 147 affected rows **title-only** (description inference is what caused the mess), writing NULL when the title supports nothing, with 7 hand-audited id overrides.
- Before → after: `lighting` 140 rows / ~6 real → 19 / 19 real; `curtains` 7 / 0 real → 0; decor-cc grid 731 → 741; furniture 965 → 978; clutter 162 → 165; detector suite 14 → 21 tests; 128 of 147 rows rewritten, 84 to NULL. Blog: 6 posts/7d; W36 adoption still 0/2 full (09-08 `sims-4-goth-accessories` is a PARTIAL — right content-type cluster, wrong angle and no brief collection link).
- Verdict: MORE DATA (read on 2026-10-06; keep if `lighting` still spot-checks 100% fixtures and the three collection grids are flat-or-up).
- Next time: when a facet looks junky, find out *why the detector produced it* before writing a cleanup script — the plural double-count was a general bug affecting every rule with redundant singular/plural spellings, and a data-only fix would have left it re-poisoning the catalog on every scrape. Also: 84 rows went NULL and many are room sets ("Sleek Sims 4 Kitchen", "Kivik Living Room Part 1", "Grunge Kitchen") — the `furniture` rule has no bare room-name keywords, so ~40 of them are recoverable in a second T0 pass. Operational note: the shared `node_modules` symlink into Quinn's worktree emptied mid-run again (`next: command not found`); `rm -f node_modules && npm ci` took 7s and unblocked it — do it pre-emptively next run.

## 2026-09-07
- Tried: decor-cc collection page (T0, PR #51) — `contentTypeIn ['decor','plants','rugs','wall-art']` = 731 SFW Sims 4 mods, the largest un-paged content type in the 15,888-mod catalog; excluded `lighting` (140) and `curtains` (7) as junk-tagged (top `lighting` rows are a GShade preset, a skin overlay and a Ford Crown Victoria). Also fixed 4 dangling `related` slugs (clutter, holidays-cc, furniture-cc) that the renderer dropped silently, and wired makeup-cc's missing `blogUrl`, which had left `canonical-trailing-slash.test.ts` red on origin/main since PR #32 (2026-09-04).
- Before → after: collection routes 17 → 18; /games/sims-4/clutter/ related cards 1 → 3; canonical-trailing-slash suite red → 15/15 green; decor-cc engaged sessions 0 (page did not exist) → read 2026-10-05; GSC baseline ~104 impressions at pos 20–42 for 5 clicks across the decor cluster (28d to 09-04). Blog: 5 posts/7d, latest sims-4-cardi-b-cc is not a W36/W37 brief topic (E12 adoption 0/2).
- Verdict: MORE DATA (read on 2026-10-05; keep if ≥200 engaged sessions or ≥5 favorites from the page).
- Next time: run the touched page-type's existing test suite against origin/main *before* writing code — the red test was 3 days old and nobody had run it. When folding facets into a `contentTypeIn` collection, sample the top rows of every facet; row count alone would not have caught that 2 of 6 decor-adjacent facets are junk. Chose T0 decor-cc over the T1 creator page because a T1 queues until tomorrow and moves no number this week.

## 2026-09-01
- Tried: nothing yet — team chartered today. Read `../charter.md`, `../autonomy.md`, `../operating-model.md`, `../targets.json`, and `reports/growth/fact-base-2026-09-01.md` before your first move.
- Before → after: baseline in `../targets.json`
- Verdict: —
- Next time: your first move should be the top item in your agent file's "levers" list unless the scoreboard shows a 🔴 in your area.
