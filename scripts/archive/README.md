# Archived Scripts

This directory contains archived test and debug scripts that are no longer needed for active development but are preserved for reference.

## Contents

### Test Scripts
- `test-lamps.ts` - Testing lamp mod searches
- `test-witch-cc.ts` - Testing witch CC mod functionality
- `test-lighting-mods.ts` - Testing lighting mod searches
- `test-amazon-api.ts` - Testing Amazon API integration
- `test-amazon-scraper.ts` - Testing Amazon product scraping
- `test-ai-scraper.ts` - Testing AI-powered scraping
- `test-scraper.ts` - General scraper testing
- `test-extraction.ts` - Testing data extraction
- `test-redis.ts` - Testing Redis connection/functionality
- `test-guide-exclusion.ts` - Testing guide exclusion logic
- `test-privacy-aggregator.ts` - Testing privacy aggregator features
- `test-affiliate-research.ts` - Testing affiliate research functionality

### Debug Scripts
- `debug-extraction.ts` - Debugging data extraction issues
- `debug-users-subscriptions.ts` - Debugging user subscription data

## Usage

These scripts were used during development for testing specific features. If you need to run them, they may require updating imports or dependencies as the codebase has evolved.

```bash
# Example (may need adjustments):
npx tsx scripts/archive/test-scraper.ts
```

## Note

The `ralph/` subdirectory contains additional archived scripts from a specific cleanup task and should be considered separately.
